# CLAUDE.md

**The instructions for this repository live in [`AGENTS.md`](AGENTS.md). Read that
file and treat it as this one.**

This file is a pointer and holds no guidance of its own. Claude Code is not the
only agent that works here, so the instruction set is kept in the agent-neutral
file that every tool reads, rather than duplicated per vendor — a second copy
drifts, and a drifted copy is worse than no copy, because it is still believed.
`AGENTS.md` was 156 commits stale when this split was made.

Nothing below the pointer. If you are about to add project guidance here, add it
to `AGENTS.md` instead.
