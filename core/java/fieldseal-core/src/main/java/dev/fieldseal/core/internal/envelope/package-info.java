/**
 * Module {@code envelope} (docs/09 §1, §4): header, parse, serialize,
 * {@code isCiphertext}, {@code BufferLimits} and the {@code Operand} length seam
 * (spec §3; docs/27 §6.2).
 *
 * <p>Dependencies: {@code registry} and {@code errors} only (docs/09 §1).
 */
package dev.fieldseal.core.internal.envelope;
