/**
 * Module {@code blindindex} (docs/09 §1, §7): the IDFs (Argon2id,
 * HMAC-SHA-512), truncation, normalizers and the Unicode tables (spec §7).
 *
 * <p>Dependencies: {@code registry} and {@code errors} only (docs/09 §1).
 */
package dev.fieldseal.core.internal.blindindex;
