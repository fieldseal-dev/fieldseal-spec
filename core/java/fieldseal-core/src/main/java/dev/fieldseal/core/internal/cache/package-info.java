/**
 * Module {@code cache} (docs/09 §1, §8.3): the DEK cache, with max-age, max-uses
 * (a {@code long}, docs/27 §4) and erasure on eviction (spec §5.5).
 *
 * <p>Dependencies: {@code errors} only (docs/09 §1).
 */
package dev.fieldseal.core.internal.cache;
