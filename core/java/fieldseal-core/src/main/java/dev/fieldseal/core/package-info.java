/**
 * Module {@code api} (docs/09 §1): the client, the five synchronous operations
 * and {@code warm} (spec §11.1; docs/27 §4).
 *
 * <p>Dependencies: {@code api} may depend on every module. No module depends on
 * {@code api}.
 */
package dev.fieldseal.core;
