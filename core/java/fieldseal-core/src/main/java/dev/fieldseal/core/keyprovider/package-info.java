/**
 * Module {@code keyprovider} (docs/09 §1, §8): the {@code KeyProvider} SPI and
 * the Static, Derived and Envelope providers (spec §8). Exported, because callers
 * implement the SPI.
 *
 * <p>Dependencies: {@code errors} only.
 */
package dev.fieldseal.core.keyprovider;
