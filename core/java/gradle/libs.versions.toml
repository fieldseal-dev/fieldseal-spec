# Versions resolved from Maven Central on 2026-09-23, not from memory.
# Test-only: none of these ships in a published artifact (docs/27 §2).
[versions]
junit = "5.14.4"
archunit = "1.5.0"
jackson = "2.22.3"
# Resolved from Maven Central on 2026-09-24. Test-only at S2 (the capability audit);
# it becomes fieldseal-core's one runtime dependency, for Argon2id only, at S5.
bouncycastle = "1.86"

[libraries]
junit-bom = { module = "org.junit:junit-bom", version.ref = "junit" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter" }
junit-platform-launcher = { module = "org.junit.platform:junit-platform-launcher" }
archunit = { module = "com.tngtech.archunit:archunit", version.ref = "archunit" }
jackson-databind = { module = "com.fasterxml.jackson.core:jackson-databind", version.ref = "jackson" }
bcprov = { module = "org.bouncycastle:bcprov-jdk18on", version.ref = "bouncycastle" }
