[project]
name = "fieldseal"
version = "0.1.0.dev0"
description = "Portable field-level encryption at rest -- reference Python core"
requires-python = ">=3.10"
dependencies = ["cryptography>=42"]

[project.optional-dependencies]
argon2 = ["argon2-cffi>=23"]
dev = ["pytest>=8", "hypothesis>=6", "jsonschema>=4", "mypy>=1.8", "ruff>=0.5"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fieldseal"]

[tool.ruff]
# The project config CI's lint job runs against. A user-level ruff config
# must not decide what this repository enforces.
line-length = 88
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B"]

[tool.mypy]
strict = true

[tool.pytest.ini_options]
testpaths = ["tests"]
