# Exact versions CI resolves against (pip install -c). Bump deliberately --
# a primitive changing underneath the vectors is exactly what the
# reproducibility job exists to catch, and it should fail on a chosen bump,
# not on a background drift. Matched to the versions the suite was built
# with on 2026-08-23.
cryptography==50.0.0
argon2-cffi==25.1.0
argon2-cffi-bindings==26.1.0
cffi==2.1.1
pytest==9.1.1
hypothesis==6.165.10
jsonschema==4.26.0
mypy==2.3.1
ruff==0.16.4
